---
import config from "@config";
---

{
  config.announcement ? (
    <aside class="sticky lg:absolute lg:top-4 lg:right-4 w-screen lg:max-w-xs mb-1 lg:mb-0 bg-slate-100 bg-opacity-10 p-2 lg:p-4 rounded text-xs text-white text-justify">
      <h1 class="font-bold mb-2">Announcement:</h1>
      <p class="">{config.announcement}</p>
    </aside>
  ) : (
    ""
  )
}
