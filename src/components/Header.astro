---
import config from "@config";
const {links} = config.header;
---

<header class="max-w-xl mx-auto p-6 py-12 text-stone-50 text-xl font-bold tracking-wider">
  <nav class="flex justify-center gap-4">
    {links.map((link) => <a href={link.href}>{link.text}</a>)}
  </nav>
</header>
